#!/usr/bin/env ruby

# Runs all pending downgrade actions on the archive.
# Schedule in cron with `bash -l -c 'app_root/bin/downgrade >> /dev/null 2>&1'`
# and make sure all required environment variables are set.

require File.expand_path('../../config/environment', __FILE__)

Downgrade.run
