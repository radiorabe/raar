# == Schema Information
#
# Table name: profiles
#
#  id          :integer          not null, primary key
#  name        :string           not null
#  description :text
#  default     :boolean          default(FALSE), not null
#  created_at  :datetime
#  updated_at  :datetime
#

important:
  name: Important

default:
  name: Default
  default: true

unimportant:
  name: Unimportant
